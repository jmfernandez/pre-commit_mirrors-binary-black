# Python package to install static version of black

This is a pseudo python package to install a statically linked
version of black, fetched from https://github.com/psf/black .
It can also be used within pre-commit.
